#!/usr/bin/node

const redHeader = document.querySelector('header');
const header = document.querySelector('header');

redHeader.addEventListener('click', function () {
  redHeader.style.color = '#FF0000';
});
